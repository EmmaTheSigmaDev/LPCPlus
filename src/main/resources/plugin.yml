name: ${project.name}
description: ${project.description}
version: ${project.version}
main: ${main.class}
author: ${author}
api-version: 1.13
depend: [LuckPerms]
softdepend: [PlaceholderAPI]
commands:
  lpcplus:
    description: Reload the configuration of LPCPlus.
    permission: lpcplus.reload
    usage: "Usage: /lpcplus reload"